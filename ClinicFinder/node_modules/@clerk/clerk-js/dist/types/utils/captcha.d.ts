interface RenderOptions {
    sitekey: string;
    retry: string;
    callback: (token: string) => void;
    'error-callback': (err: any) => void;
}
declare global {
    export interface Window {
        turnstile: {
            execute: (container?: string | HTMLElement | null, params?: RenderOptions) => void;
        };
    }
}
export declare function loadCaptcha(url: string): Promise<{
    execute: (container?: string | HTMLElement | null | undefined, params?: RenderOptions | undefined) => void;
}>;
export declare const getCaptchaToken: (captchaOptions: {
    siteKey: string;
    scriptUrl: string;
}) => Promise<string>;
export {};
